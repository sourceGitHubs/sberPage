import React from "react";

import styles from "./AverageCompleted.module.scss";

const AverageCompleted = () => {
  return (
    <div className={styles.title}>
      <div style={{ fontSize: "12px", color: "#939393" }}>{}</div>
    </div>
  );
};

export default AverageCompleted;
