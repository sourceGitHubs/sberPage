import { CSSProperties } from "react";

export interface insideContentTypes {
  text?: string;
  styleMean?: CSSProperties;
  styleParam?: CSSProperties;
  mean?: number | string;
  param?: string;
  num?: number;
  type?: string;
}
